import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.jancosteen.angular',
  appName: 'Angular App',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
